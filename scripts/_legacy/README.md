# Legacy conversion scripts

This folder is reserved for one-off scripts used during the JS -> TS migration.

Now that the conversion is complete, keep only scripts here that are:

- Migration-specific
- Not needed for daily development
- Useful only for historical audits or fallback checks

